# ADR 001: DAG 作为纯执行引擎

## 状态

已接受

## 决策

- DAG 不实现任何流水线逻辑，仅负责节点调度、依赖解析、并发执行、重试与状态管理。
- sempatch.py（项目根）为唯一核心编排入口，管理全部运行逻辑。

## 后果

- 流水线构建、ctx 管理、输出策略均在 sempatch.py 中。
- DAG 层不包含 run_sempatch_* 或类似业务逻辑，便于测试与扩展。
